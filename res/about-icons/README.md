# About and Help artwork

`step1.gif` through `step5.gif` are replace-in-place tutorial pages at 300×240.
`contact.gif` is the 32×32 Contact icon. All files are listed in the UI section
of `../challenge-icons/registry.template.json` so the normal icon generator
embeds them into the userscript.
