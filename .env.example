# Optional public browser build configuration.
# VITE_ means that the value is intentionally embedded into the userscript.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
VITE_SUPABASE_AUTH_REDIRECT_URL=https://skribbl.io/
VITE_GATEWAY_URL=https://skribblduels-production.up.railway.app

# Do not put server secrets in this root browser environment file.
# The future Gateway will have its own apps/gateway/.env.example.
# Comment for Re-Deployment purposes