# @skribbl-duels/telemetry-core

Typo relay integration, raw packet recording, protocol decoding, canonical lobby state and normalized telemetry production.

This package contains no visible UI. The Telemetry Inspector app adds the debug panel. The future Skribbl Duels production app will consume the same core without mounting the inspector panel.
